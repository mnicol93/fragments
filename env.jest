# This file can be commited to git, since it is only used for testing
# and won't contain any secrets.

# HTTP Port (defaults to 8080)
PORT=8080

# Disable logs in tests. If you need to see more detail, change to 'debug'
LOG_LEVEL=silent

# .httpasswd file to use in testing
HTPASSWD_FILE=tests/.htpasswd
